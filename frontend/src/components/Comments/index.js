import React from "react";

const Comments = function ({ image }) {
  //   const image = useSelector(() => image);
  console.log(image);
  return (
    <div>
      <h1>h1</h1>
    </div>
  );
};

export default Comments;
